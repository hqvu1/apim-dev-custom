# ============================================
# BFF SERVER CONFIGURATION
# ============================================
# Copy to bff/.env and customize

# Server
NODE_ENV=development
BFF_PORT=3001

# Azure ARM API Configuration (used for discovery & SAS token generation)
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_RESOURCE_GROUP=your-resource-group
APIM_SERVICE_NAME=your-apim-service-name
APIM_API_VERSION=2022-08-01

# APIM Data API Configuration (same data source as the portal's DataApiClient)
# If APIM_DATA_API_URL is not set, the BFF auto-discovers it from the ARM service description.
# APIM_DATA_API_URL=https://your-apim.management.azure-api.net
APIM_DATA_API_VERSION=2022-04-01-preview

# Development: Use mocks (no Azure auth required)
USE_MOCK_MODE=true

# Production: Use managed identity (set USE_MOCK_MODE=false)
# AZURE_CLIENT_ID=your-managed-identity-client-id
#   OR
# MANAGED_IDENTITY_CLIENT_ID=your-managed-identity-client-id

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
